invitation

receipient_email
user_id
token
dare_id
status

controller:
new create

belongs_to :sender, :class_name => 'User'
has_one :recipient, :class_name => 'User'


Users:
  invitation_id

Dare:
  invitation_id

User
  has_many :sent_invitations, :class_name => 'Invitation', :foreign_key => 'sender_id'
  belongs_to :invitation


Date has_many invitations