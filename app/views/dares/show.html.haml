%h1
  Challenge:
  = @dare.challenge.name

%h2
  Challenger:  
  = @dare.challenger.username
  %br

  Accepted by:
  = @dare.acceptor.username
  
  %br
  Started on:
  = @dare.created_at

  %br
  Finishing in:
  %time{"data-livestamp" => (@dare.created_at + 7.days).to_i}

- if @dare.vid_link?
  .embed-responsive.embed-responsive-16by9
    %iframe.embed-responsive-item{:src => "http://www.youtube.com/embed/#{@dare.vid_link}"}

- if current_user.accepted_dares.include? @dare 
  - unless @dare.times_up? && @dare.unresolved?
    = form_for([@dare.challenge, @dare]) do |f|
      = f.label :vid_link
      = f.text_field :vid_link
      = f.submit "Upload Youtube vid", class: "btn btn-success"

/ - if @dare.up_for_voting?
/   = render 'voting', {dare: @dare}
/ - elsif @dare.voting_finished?
/   = render 'voting_result', {dare: @dare}