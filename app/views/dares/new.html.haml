%h1
  Who do you want to challenge?

- if @dare.errors.any?
  %h2
    = "#{pluralize(@dare.errors.count, "error")} prohibited this dare from being saved:"
    %ul
      - @dare.errors.full_messages.each do |msg|
        %li= msg

/ = form_for([@challenge, @dare]) do |f|
/   - @dare.challenger_id = current_user.id
/   - @dare.challenge_id = @challenge.id
  
/   = f.hidden_field :challenger_id
/   = f.hidden_field :challenge_id


/   .field
/     = collection_select(:dare, :acceptor_id, User.all, :id, :username)
/   = f.submit "Bet", id: 'bet'
    
= form_tag users_search_path, method: :get, remote: true do 
  %p
    = text_field_tag :query, params[:query]
    = submit_tag "Search", name: nil, id: 'search_for_users'

#user_search_result

