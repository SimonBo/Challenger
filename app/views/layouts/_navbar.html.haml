%nav.navbar.navbar-default{:role => "navigation"}
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      = link_to 'Challenger', challenges_path, class: "navbar-brand"
    / Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li
          = link_to 'Choose your destiny!', challenges_path
        / %li.dropdown
        /   %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
        /     Rankings
        /     %span.caret
        /   %ul.dropdown-menu{:role => "menu"}
        %li
          %a#notification-link{:href => "#notifications"} Notifications
        %li
          = link_to 'Challenge aficionados', users_path
        %li
          = link_to "These need your vote!", dares_voting_path
      / %form.navbar-form.navbar-left{:role => "search"}
      /   .form-group
      /     = form_tag challenges_path, method: :get do 
      /       %p
      /         = text_field_tag :query, params[:query]
      /         = submit_tag "Search", name: nil
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              = current_user.username
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li
                = link_to 'Profile', current_user
              %li
                = link_to 'Edit account', edit_user_registration_path
              %li.divider
              %li
                = link_to 'Sign out', destroy_user_session_path, :method=>'delete'
        - else
          = link_to new_user_session_path do
            %button.btn.btn-sm.btn-default Sign in
          = link_to new_user_registration_path do
            %button.btn.btn-sm.btn-default Sign up

        