challenge others -> create new dare (status: invitation-unconfirmed)-> check if the user email is in db -> if not -> send email to invitee ->
link to invitation_show(with dare id and token) -> Accept --> Sign_up // dare.status = 'Accepted'



Dare.rb

def invitation_expired?
  If self.status == 'invitation-unconfirmed' and self.created_at >= 5.days.ago
    self.status = 'invitation-no-response'
  end
end

Controller

invitation_show ( params -> dare_id, token)

invitation controller?